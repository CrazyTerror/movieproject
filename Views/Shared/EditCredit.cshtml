@model MovieProject.Models.FilmItemCredits
@{
    ViewData["Title"] = "Edit";
}
<h2>@ViewData["Title"]</h2>

<form asp-action="EditCredit" method="post">
    <input type="hidden" name="Id" value="@Model.Id" />
    <div class="form-group">
        <label asp-for="Character"></label>
        <div><span asp-validation-for="Character" class="text-danger"></span></div>
        <input asp-for="Character" class="form-control" />
    </div>
    <div class="text-center">
        <button class="btn btn-primary" type="submit">Save</button>
        <a asp-action="Index" class="btn btn-secondary">Cancel</a>
    </div>
</form>
