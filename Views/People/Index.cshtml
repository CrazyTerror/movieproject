@model List<MovieProject.Models.Person>

@{
    ViewData["Title"] = "About People";
}
<h2>@ViewData["Title"]</h2>

@if (Model.Count > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Person</th>
                <th scope="col">Year</th>
                <th scope="col"></th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (Person p in Model)
            {
                <tr>
                    <td>
                        <a asp-controller="People" asp-action="Details" asp-route-slug="@p.Slug">@p.FirstName @p.Surname</a>
                    </td>
                    <td>
                        <form asp-action="Delete" asp-route-id="@p.Id" method="post">
                            <a asp-action="Edit" class="btn btn-sm btn-warning" asp-route-id="@p.Id">
                                Edit
                            </a>
                            <input type="hidden" name="Id" value="@p.Id" />
                            <button type="submit" class="btn btn-danger btn-sm">
                                Delete
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

<div class="text-center">
    <a asp-controller="People" asp-action="Create" class="btn btn-primary">Add Season</a>
</div>
